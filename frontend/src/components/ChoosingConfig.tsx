import { useState, useEffect, React } from "react";
import api from "../api"

const ChoosingConfig = () => {
  const [frames, setFrames] = useState<string[]>([]);
  const [selectedFrames, selectedFrames] = useState<Set<string>>();

  useEffect()=>{
    api.
  },[])
  return <div>Choosing Config</div>;
};

export default ChoosingConfig;
