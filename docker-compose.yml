services:
  frontend:
    build:
      context: ./frontend/
    ports:
      - "5173:5173"
    container_name: frontend
    depends_on:
      - backend

  backend:
    build:
      context: ./backend/
    ports:
      - "5000:5000"
    container_name: backend
